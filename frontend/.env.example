# Copie este arquivo para .env.local antes de rodar o frontend:
#   cp frontend/.env.example frontend/.env.local

# Endereco da API. O prefixo NEXT_PUBLIC_ e obrigatorio para que a variavel
# chegue ao navegador, que e de onde as chamadas partem.
NEXT_PUBLIC_API_URL=http://localhost:8081
