# Copie este arquivo para .env antes de rodar o backend:
#   cp backend/.env.example backend/.env

DB_HOST=127.0.0.1
DB_PORT=3307
DB_DATABASE=ultralims
DB_USERNAME=ultralims
DB_PASSWORD=ultralims

# Prefixo usado na geracao do codigo da amostra: {PREFIXO}-{ANO}-{SEQUENCIAL}
CODIGO_PREFIXO=ISABELLA
